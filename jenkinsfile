pipeline {
    agent any

    stages {

        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Compile') {
            steps {
                bat 'javac index.java'
            }
        }

        stage('Run') {
            steps {
                bat 'java index'
            }
        }
    }
}